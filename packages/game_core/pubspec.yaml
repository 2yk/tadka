name: game_core
description: >-
  Pure-Dart rules engine for Project Tadka: seeded RNG, pattern detection,
  scoring, run state machine, economy. ZERO Flutter imports — this package must
  stay headless and fully unit-testable (build spec §3).
version: 0.1.0
publish_to: none
resolution: workspace

environment:
  sdk: ^3.12.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
